@using CarZone.Models.ViewModels
@model List<ModeloVeiculoVM>
@{
    ViewData["Title"] = "Lista de Modelos";
}

<div class="text-center">
    <p class="display-6">Lista de Modelos</p>
    <br />
    <a class="btn btn-outline-danger btn-sm" asp-controller="ModeloVeiculos" asp-action="Criar">Adicionar Modelo</a>
    <br />
    <br />


    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="close">X</button>
            @TempData["MensagemSucesso"]
        </div>
    }

    @if (TempData["MensagemErro"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="close">X</button>
            @TempData["MensagemErro"]
        </div>
    }


    <div class="text-center">
        <table class="table">
            <thead>
                <tr>
                    <th class="d-none">Id</th>
                    <th scope="col">Nome de Modelo</th>
                    <th scope="col">Marca</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (ModeloVeiculoVM modelo in Model)
                    {
                        <tr>
                            <th class="d-none">@modelo.Id</th>
                            <td>@modelo.NomeModelo</td>                            
                            <td>@modelo.Marca</td> 
                            <td>
                                <a type="button" class="btn btn-outline-dark" asp-controller="ModeloVeiculos" asp-route-id="@modelo.Id" asp-action="EditarModelo">Editar</a>
                                <a type="button" class="btn btn-outline-dark" asp-controller="ModeloVeiculos" asp-route-id="@modelo.Id" asp-action="ApagarConfirmacao">Apagar</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>